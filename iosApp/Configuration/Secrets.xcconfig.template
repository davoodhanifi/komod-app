// Copy this file to Secrets.xcconfig and fill in your real values.
// Secrets.xcconfig is gitignored and must NOT be committed.
//
// Supabase credentials are NOT needed here — they are baked into the
// shared Kotlin binary at build time via BuildKonfig (source: local.properties).
//
// Add any iOS-only secrets here (e.g. API keys only needed by native iOS code).
